<%= form_with(model: manufacture_material, local: true) do |form| %>
  <% if manufacture_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manufacture_material.errors.count, "error") %> prohibited this manufacture_material from being saved:</h2>

      <ul>
      <% manufacture_material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :material_id %>
    <%= form.number_field :material_id %>
  </div>

  <div class="field">
    <%= form.label :manufacture_id %>
    <%= form.number_field :manufacture_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
