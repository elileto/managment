<%= form_with(model: order_date, local: true) do |form| %>
  <% if order_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_date.errors.count, "error") %> prohibited this order_date from being saved:</h2>

      <ul>
      <% order_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_placed %>
    <%= form.datetime_select :order_placed %>
  </div>

  <div class="field">
    <%= form.label :order_received %>
    <%= form.datetime_select :order_received %>
  </div>

  <div class="field">
    <%= form.label :order_complete %>
    <%= form.check_box :order_complete %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
