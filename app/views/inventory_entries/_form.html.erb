<%= form_with(model: inventory_entry, local: true) do |form| %>
  <% if inventory_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inventory_entry.errors.count, "error") %> prohibited this inventory_entry from being saved:</h2>

      <ul>
      <% inventory_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :inventory_id %>
    <%= form.number_field :inventory_id %>
  </div>

  <div class="field">
    <%= form.label :entry_date %>
    <%= form.datetime_select :entry_date %>
  </div>

  <div class="field">
    <%= form.label :number_of_units %>
    <%= form.text_field :number_of_units %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
